from typing import Optional

import pandas as pd
import datetime


def get_expenses_for_3_months_by_category(operation: pd.DataFrame, category: str, date: Optional[str] = None) -> pd.DataFrame:
    pass